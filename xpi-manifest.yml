---
xpis:
  # TODO: create a manifest during the build.
  # repository, revision, name, version/metadata from package.json,
  # artifact shas + filesize?
  - name: shield-study-helper-addon
    repo: https://github.com/mozilla/shield-studies-addon-utils
    directory: misc/shield-study-helper-addon
    active: true
    # private-repo: defaults to false
    # TODO allowed-branches
    # TODO allow-reviewed-pr
    artifacts:
      # TODO standardize name
      # softlink pointing at @qa-shield-study-helper-1.0.0.xpi
      #- linked-addon.xpi
      - '@qa-shield-study-helper-1.0.0.xpi'
    # TODO naming? switch signing key based on value
    addon-type: standard
    install-type: npm
    treeherder-symbol: helper
#  - name: private-helper
#    repo: git@github.com:escapewindow/test-xpi
#    directory: misc/shield-study-helper-addon
#    active: true
#    private-repo: true
#    # TODO allowed-branches
#    # TODO allow-reviewed-pr
#    artifacts:
#      # TODO standardize name
#      # softlink pointing at @qa-shield-study-helper-1.0.0.xpi
#      - linked-addon.xpi
#    # TODO naming? switch signing key based on value
#    addon-type: standard
#    install-type: npm
#    treeherder-symbol: private-helper
  - name: multipreffer
    repo: https://github.com/mozilla/multipreffer
    active: true
    artifacts:
      # TODO standardize name
      - dist/multipreffer-2.0.zip
    addon-type: standard
    install-type: npm
  - name: fingerprinting-and-cryptomining-protections-retention-study
    repo: https://github.com/mozilla/fingerprinting-cryptomining-retention-study
    active: true
    artifacts:
      - dist/fingerprinting_and_cryptomining_protections_retention_study-1.1.zip
    addon-type: standard
    install-type: npm
    treeherder-symbol: fingerprint
